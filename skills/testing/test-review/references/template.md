# テストレビュー記録: <テスト対象名> / <工程>

- レビュー対象: <対象成果物のパス（複数可）>
- 実施日: <YYYY-MM-DD>
- 判定: <通過 / 条件付き通過 / 差し戻し>（利用者判定。機械検査 NG による差し戻しはその旨を明記）
- 機械検査: OK <n> / NG <n> / SKIP <n>（review-check.sh <工程>。SKIP の理由が判定に影響するなら 1 行添える）

## 未解消の指摘

条件付き通過・差し戻しで残った指摘のみを保持する。解消した指摘は削除し（「解消済み」注記で
残さない）、全て解消したらセクションごと削除する（判定と実施日だけの薄い記録になる）。

| # | 指摘（何が問題か） | 重み | 解消の条件 |
|---|---|---|---|
| 1 | <指摘内容> | must/want/nit | <どうなれば解消とみなすか> |

## 次のステップ

- 差し戻し時: 対象工程スキル（`/test-<工程> <テスト対象名>`）の再実行または利用者の修正後、
  `/test-review <テスト対象名> <工程>` を再実行する。
- 通過/条件付き通過時: 対象工程スキルへ戻り完了宣言する。
