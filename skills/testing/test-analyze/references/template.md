# テスト分析: <テスト対象名>

- 対象: <テスト対象の範囲。モジュール/機能/バージョン>
- 作成日: <YYYY-MM-DD>
- テストベース: <参照した仕様書・設計書・コード・test-plan.md へのパス/リンク>
- リスク入力: <test-plan.md のリスク評価を使ったか。無ければ「なし（/test-plan 未実施）」>

## 1. スコープ

- 分析対象: <テスト条件を洗い出す範囲>
- 対象外: <この分析で扱わない範囲と理由>

## 2. テスト条件一覧

テストで検証すべき項目・観点を識別する。具体的な入力値・期待結果はここでは書かない
（それは test-design で導出する）。

| ID | テスト条件（何を検証するか） | 切り口 | 対応テストベース | 対応リスク # | 優先度 |
|---|---|---|---|---|---|
| TC-01 | <検証すべき振る舞い・観点> | 機能/境界/状態/例外/非機能/依存 | <要求 ID / コード箇所> | <R1 等 / -> | 高/中/低 |
| TC-02 | | | | | |

- 切り口: 機能要求 / 入力の区分（正常・異常・境界・同値） / 状態・遷移 / 例外・エラー処理 /
  非機能 / 相互作用・依存。
- 優先度は test-plan のリスク評価を主根拠に付ける。リスク入力が無い場合は暫定（下記注記）。

## 3. 優先度の根拠

- 高優先度の条件と、それを高くした根拠（対応リスク / 重要度 / 変更頻度）。
- リスク入力が無い場合の注記: <暫定優先度である旨。`/test-plan <テスト対象名>` の実行を推奨>

## 4. カバレッジの見立て

- 網羅する観点と、意図的に除外した観点（除外理由つき）。
- 既存テストで既にカバー済みの条件（重複回避のため）。

## 5. 改善提案

分析中に気づいたテスト対象・仕様・テスタビリティの問題を提示する（修正はしない）。

- <仕様の矛盾・曖昧さ / 抜けている要求 / 観測不能な状態 / テスト困難な構造 など>

## 6. 次のステップ

- `/test-design <テスト対象名>` を実行し、各テスト条件からテストケースを導出する。
