# テスト設計書: <テスト対象名>

- 対象: <テスト対象の範囲。モジュール/機能/バージョン>
- 作成日: <YYYY-MM-DD>
- 入力: <参照した test-analysis.md / test-plan.md / 仕様・コードへのパス/リンク>
- ケース一覧: [`test-case.md`](test-case.md)（本設計に基づく具体テストケース）
- 初見者向けサマリ: `mini-test-design.md` / `mini-test-case.md`（レビュー収束後に作成）

## 1. 採用テスト技法と選定根拠

各テスト条件にどの技法を当て、なぜそれを選んだかを残す（この節は必須）。

| 対応テスト条件 | 採用技法 | 選定根拠（対象の特性）| カバレッジ基準 |
|---|---|---|---|
| TC-01 | 境界値分析 | <値域を持つ入力のため> | 各境界の内側/境界/外側 |
| TC-02 | デシジョンテーブル | <複数条件の組合せで結果が変わるため> | 全ルール |

## 2. カバレッジの確認

- 各技法のカバレッジ基準を満たしたか（例: 全境界を網羅 / 全ルールを網羅 / 全分岐を網羅）。
- 意図的に省いた組合せと、その理由（ケース爆発の抑制など）。

## 3. 自動/手動の区分基準

`test-case.md` の各ケースに付けた区分（自動/手動）の判定根拠を残す。

| 実行手段 | 実行方法（コマンド・CI）| 区分 |
|---|---|---|
| <例: Unit テスト> | <例: `make unit-test`・CI 組み込み済み> | 自動 |
| <例: 別リポジトリのインフラ差分レビュー> | <例: terraform plan の目視確認> | 手動 |

- データ準備等に人手が要る半自動的な検証手段は、ケースの区分値には出さずここに整理する。
- 区分は設計時点の判定。実装時の食い違いは test-implement が `test-case.md` を更新して正とする。

## 4. 改善提案

設計中に気づいたテスト対象・仕様・テスタビリティの問題を提示する（コードの修正はしない）。

- <期待結果が仕様から確定できない / 境界が曖昧 / 状態が観測不能でケース化できない など>
- 運用: 利用者の決定を得て仕様書等へ反映した提案は削除する（「反映済み」注記で残さない）。
  全提案が解消したらセクションごと削除し、後続の番号を詰める。

## 5. 次のステップ

- `/test-implement <テスト対象名>` を実行し、テストケースをコード or 手順書へ具体化する。
