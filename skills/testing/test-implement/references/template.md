# テスト手順書: <テスト対象名>

- 対象: <テスト対象の範囲。モジュール/機能/バージョン>
- 作成日: <YYYY-MM-DD>
- 前提資料: <参照した test-cases.md / test-plan.md 等へのパス>

※ このテンプレは **手動テスト** の手順書用。自動テストはプロジェクトのテストコード配置規約に従う。

## 1. 環境準備

- 前提環境: <OS / ブラウザ / バージョン / 依存サービス など>
- セットアップ手順: <アプリ起動・ログイン・初期状態の作り方>
- テストデータ投入: <投入するデータと投入方法>
- 後片付け: <実施後に戻す・削除する手順>

## 2. テスト手順

test-cases.md の各ケースを、実施者が迷わない粒度で具体化する。

### TP1: <ケース名（対応する test-cases.md のケース #）>

- 前提条件: <このケースを始める前の状態>
- 手順:
  1. <操作>
  2. <操作>
- 入力データ: <投入値・アカウント・パラメータ>
- 期待結果: <観測可能な合否判定基準>

### TP2: <ケース名>

- 前提条件:
- 手順:
  1.
- 入力データ:
- 期待結果:

## 3. テストデータ一覧

| データ名 | 値/内容 | 用途（対応 TP #） |
|---|---|---|
| <名前> | <値> | <TP1 など> |

## 4. 改善提案

具体化中に気づいたテスト対象・仕様・テスタビリティの問題を提示する（修正はしない）。

- <テスト困難な構造 / 観測不能な状態 / 非決定的な挙動 / 差し替え不能な依存 など>

## 5. 次のステップ

- `/test-execute <テスト対象名>` を実行してテストを実施・記録する。
