# テストモニタリング定義: <テスト対象名>

- 対象: <テスト対象の範囲。モジュール/機能/バージョン>
- 作成日: <YYYY-MM-DD>
- 入力: <test-plan.md 等へのパス（完了基準の紐づけ先）>
- CI/ツール構成: <調査で判明した CI プロバイダ・ランナー・カバレッジツール>

## 1. 目的

- このモニタリング基盤で何を可視化するか。test-plan のどの完了基準に紐づくか。
- ※ このドキュメントは **計測基盤の定義** であり、集めたデータの分析・傾向判断は範囲外
  （評価は test-report が担当）。

## 2. メトリクス定義

| メトリクス | 定義 | 取得元 | しきい値 | 対応する完了基準 |
|---|---|---|---|---|
| <例: ラインカバレッジ> | <割合の算出方法> | <カバレッジツールの出力> | <例: ≥80%> | <test-plan の基準 #> |
| <例: 失敗率> | | | | |
| <例: flaky 率> | | | | |

## 3. 計測基盤（実装物）

- CI 設定: <追加/拡張したジョブ・ファイルパス>
- 集計スクリプト: <パスと役割。入力（ランナー出力）→ 出力（メトリクス値）>
- 可視化: <バッジ / PR コメント / 成果物アップロード のうち採用したもの>

## 4. 運用メモ

- 計測は上記基盤が **自動で** 行う（AI が継続監視するのではない）。
- 数値の評価・完了判定は `/test-report` で行う。

## 5. 改善提案

基盤構築中に気づいたテスト対象・仕様・テスタビリティ・計測可能性の問題を提示する（修正はしない）。

- <取得できないメトリクス / 計測不能な完了基準 / CI に載せられないテスト など>

## 6. 次のステップ

- 計測が回り始めたら、結果の評価・完了判定は `/test-report <テスト対象名>` で行う。
