---
name: commit-plan
description: '実装計画・リファクタリング計画・レビュー対応計画を立てるとき（特に plan モード）に必ず参照するコミット計画ルール。ExitPlanMode で plan を提示する前に、plan 本文へコミット計画セクションが含まれていなければならない。「コミット計画を立てて」「実装計画を立てて」「planを立てて」「実行計画を作って」「リファクタリング計画」と依頼される、plan モードで計画を組み立てる、複数の独立した修正をまとめるか分けるか計画段階で判断する等の場面で、ユーザーが「コミット」に言及していなくても必ず発火する。粒度は論理的に独立した修正単位で分割し、メッセージは Conventional Commits 形式（type(scope): description）で計画する。コミットの実施（素材収集・メッセージ確定・git commit 実行）は commit-flow スキルが担う。'
---

# コミット計画ルール

## このスキルの目的

**plan モードで実装計画／リファクタリング計画／レビュー対応計画を立てる際は、plan 本文に必ずコミット計画を含める**。実装ステップだけの plan で実装に入らない。

コミットの実施（素材収集・メッセージ確定・`git commit` 実行）は本スキルの範囲外で、commit-flow スキルが担う。

## 1. コミット計画を plan 本文に明示する

実装ステップと対になる「コミット計画」セクションを plan に組み込む。`ExitPlanMode` で提示する plan に**含まれていない状態で実装に入らない**。

```markdown
## 実装ステップ
1. ドメインモデルに `Foo` を追加
2. リポジトリ層に永続化メソッドを追加
3. ユースケース層から呼び出し
4. テスト追加

## コミット計画
1. `feat(domain): Foo モデルを追加`
2. `feat(infra): FooRepository に永続化メソッドを追加`
3. `feat(usecase): Foo 永続化ユースケースから呼び出し`
4. `test(usecase): Foo 永続化のユースケーステストを追加`
```

## 2. 粒度判定は plan 段階で済ませる

§4 の基準を立案時に適用する。「実装してみないと粒度が分からない」と先送りしない。

## 3. コミット数を理由に統合しない

分割か統合か未確定で残す場合、粒度に意味があるなら分割を推奨案として書く。コミット数増加はレビュアー追跡性・ロールバック容易性を確保するメリットの方が大きい。

## 4. コミット粒度の判断基準

**分割する**: 別の関心事／片方だけ revert したくなる可能性／別 discussion・別 issue の対応。
**統合する**: 同じ論理変更を機械的に行数で割っただけ／片方だけ戻すと壊れる／同じ discussion 内の一連の修正。

「コミット数が増えるから 1 つにまとめる」は採用しない。

## 5. 計画時のメッセージ形式

計画に書くメッセージは [Conventional Commits 1.0.0](https://www.conventionalcommits.org/) の 1 行形式:

```
<type>(<scope>): <description>
```

主要 type: `feat` / `fix` / `refactor` / `test` / `docs` / `chore` / `build` / `ci` / `perf` / `style` / `revert`

計画段階ではこの 1 行があれば足りる。body／footer・破壊的変更の書き方・type 選択の詳細は、実施時に commit-flow スキルの `references/conventional-commits.md` で判断する。

## 関連スキル

- commit-flow: コミットの実施（素材収集スクリプト・メッセージ確定・粒度の実施時判断・`git commit` 実行）
- parallel-worktree: 並列タスク分解時、各エージェントの plan に本スキル準拠のコミット計画を含める
