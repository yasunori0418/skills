# example-skill reference

Put detailed, on-demand documentation here. Agents load this file only when the
task needs it, keeping the main `SKILL.md` small (progressive disclosure).

Keep references one level deep from `SKILL.md` and each file focused.
